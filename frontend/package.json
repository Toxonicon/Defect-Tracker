{
  "name": "defect-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "nuxt dev --hostname 0.0.0.0 --port 3000",
    "build": "nuxt build",
    "start": "nuxt start --hostname 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "nuxt": "^3.8.0"
  }
}
